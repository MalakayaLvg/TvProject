{% extends 'base.html.twig' %}

{% block title %}Liste des films{% endblock %}

{% block body %}
<div class="adminBackground min-vh-100">

    {% include 'admin/navbar/navbar.html.twig' %}

    <div class="container">

        <div class="users-table-container">
            <div class="header d-flex justify-content-between">
                All films: {{ films|length }}
                <div>
                    <select class="form-select" aria-label="Default select example">
                        <option selected>Sort by</option>
                        <option value="1">ID</option>
                        <option value="2">Title</option>
                        <option value="3">Rate</option>
                    </select>
                </div>
                <a class="btn btn-warning" href="{{ path('app_film_create') }}">Add film</a>
            </div>

            <table class="users-table">
                <thead>
                <tr>
                    <th>ID</th>
                    <th>Title</th>
                    <th>Category</th>
                    <th>Description</th>
                    <th>Action</th>
                </tr>
                </thead>
                <tbody>
                {% for film in films %}
                    <tr>
                        <td>{{ film.id }}</td>
                        <td>{{ film.title }}</td>
                        <td>{{ film.description }}</td>
                        <td>Category</td>
                        <td>
                            <a href="{{ path('app_film_show', {id: film.id}) }}"><i class="bi bi-eye p-2"></i></a>
                            <a href="{{ path('app_film_edit', {id: film.id}) }}"><i class="bi bi-pencil p-2"></i></a>
                            <a href="{{ path('app_film_delete', {id: film.id}) }}"><i class="bi bi-trash p-2"></i></a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
