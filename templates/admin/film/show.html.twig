{% extends 'base.html.twig' %}

{% block title %}Détails du film - {{ film.title }}{% endblock %}

{% block body %}

<div class="adminBackground min-vh-100">
    {% include 'admin/navbar/navbar.html.twig' %}
<<<<<<< templates/admin/film/show.html.twig
    <div class="container mt-3">
        <h1 class="my-4 text-center">Détails du film : <span class="text-primary">{{ film.title }}</span></h1>

        <div class="d-flex justify-content-between mb-4">
            <a href="{{ path('app_film') }}" class="btn btn-secondary">Retour à la liste des films</a>
            <form method="post" action="{{ path('app_film_delete', {id: film.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce film ?');">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ film.id) }}">
                <button class="btn btn-danger">Supprimer le film</button>
                <a href="{{ path('app_film_edit', {id: film.id}) }}" class="btn btn-warning">Éditer le film</a>
            </form>
        </div>

        <table class="table table-hover table-striped border">
            <tbody>
            <tr>
                <th scope="row">ID</th>
                <td>{{ film.id }}</td>
            </tr>
            <tr>
                <th scope="row">Titre</th>
                <td>{{ film.title }}</td>
            </tr>
            <tr>
                <th scope="row">Description</th>
                <td>{{ film.description }}</td>
            </tr>
            <tr>
                <th scope="row">Date de publication</th>
                <td>{{ film.publishDate ? film.publishDate|date('Y-m-d') : 'N/A' }}</td>
            </tr>
            <tr>
                <th scope="row">Durée</th>
                <td>{{ film.runtime }} minutes</td>
            </tr>
            <tr>
                <th scope="row">Note critique</th>
                <td>{{ film.criticalRate is not null ? film.criticalRate : 'Non noté' }}</td>
            </tr>
            </tbody>
        </table>

        <h2 class="my-4">Commentaires</h2>

        <div class="comments-section mb-5">
            {% if film.comments is not empty %}
                <ul class="list-group">
                    {% for comment in film.comments %}
                        <li class="list-group-item d-flex justify-content-between align-items-start bg-light mb-3 rounded">
                            <div class="comment-body">
                                <p class="mb-2">{{ comment.content }}</p>
                                <small class="text-muted">Posté le {{ comment.createdAt|date('Y-m-d H:i') }}</small>
                            </div>
                        </li>
                    {% endfor %}
                </ul>
            {% else %}
                <div class="alert alert-info">Aucun commentaire pour ce film.</div>
            {% endif %}
        </div>


    </div></div>
{% endblock %}
=======
<div class="adminBackground min-vh-100">
    {% include 'admin/navbar/navbar.html.twig' %}
<div class="container">

    <div class="py-4"></div>
    <div class="filmCarouselPrincipal">

    </div>

    <div class="py-5 d-flex">
        <div class="d-flex flex-column">
            <div class="filmVerticalImage"></div>
            <div class="d-flex justify-content-between py-3">
                <h3>Vertical Image</h3>
                <div class="d-flex">
                    <h3><i class="bi bi-trash px-2"></i></h3>
                    <h3><i class="bi bi-pencil px-2"></i></h3>
                </div>

            </div>
        </div>

        <div class="mx-5">
            <h2>{{ film.title }}</h2>
            <h3>Plateform: </h3>
            <h3>Catégories: </h3>
            <h3>Language: </h3>
            <h3>Pubish Date: {{ film.publishDate|date('Y-m-d')}} </h3>
            <h3>Critical Rate: {{ film.criticalRate }} / 5 </h3>
            <p>{{ film.description }}</p>
        </div>
    </div>

    <div class="d-flex w-100 pt-5">
        <div class="w-25">
            <h3>Comments</h3>
        </div>
        <div class="w-75">
            <table class="users-table">
                <thead>
                <tr>
                    <th>User</th>
                    <th>Comment</th>
                </tr>
                </thead>
                <tbody>
                <tr>
                    <td>user</td>
                    <td>comment</td>
                </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div class="d-flex justify-content-center py-4">
        <a href="#" class="btn btn-warning">View all comments</a>
    </div>

    <div class="d-flex w-100 pt-5">
        <div class="w-25">
            <h3>Rates :</h3>
        </div>
        <div class="w-75">
            <table class="users-table">
                <thead>
                <tr>
                    <th>User</th>
                    <th>Rates</th>
                </tr>
                </thead>
                <tbody>
                <tr>
                    <td>user</td>
                    <td>rate</td>
                </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div class="d-flex justify-content-center py-4">
        <a href="#" class="btn btn-warning">View all rates</a>
    </div>

    <div class="py-5 d-flex justify-content-center">
        <a href="#" class="btn btn-warning px-4 mx-4">edit</a>
        <a href="#" class="btn btn-danger px-4 mx-4">Delete</a>
    </div>


</div>
</div>
{% endblock %}
>>>>>>> templates/admin/film/show.html.twig
